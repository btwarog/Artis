query BrowseArtistsQuery ($searchQuery: String!, $cursor: String) {
    search {
        artists(query: $searchQuery, first: 15, after: $cursor) {
            nodes {
                ...ArtistBasicFragment
            }
            pageInfo {
                hasNextPage
                endCursor
            }
            totalCount
        }
    }
}