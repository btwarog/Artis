query BrowseArtistsQuery {
    search {
        artists(query: "Nirvana", first: 15, after: null) {
            nodes {
                ...ArtistBasicFragment
            }
            pageInfo {
                hasNextPage
                endCursor
            }
            totalCount
        }
    }
}